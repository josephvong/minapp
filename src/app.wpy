<style lang="less">
view,ul,li {
  box-sizing: border-box;
}
.container {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  box-sizing: border-box;
}
.clearfix{
  display:inline-block 
}
.clearfix:after{
  content: "";
  display: block;  
  height: 0; 
  line-height: 0;
  clear: both;
  visibility: hidden
} 
  
</style>

<script>
import wepy from 'wepy'
import 'wepy-async-function'
import {kachaAuthApi} from 'api/kachaAuthApi/index'
export default class extends wepy.app {
  config = {
    pages: [
      'pages/search/index'
      //'pages/game',
      //'pages/welcome',
      //'pages/index',
    ],
    window: {
      backgroundTextStyle: 'light',
      navigationBarBackgroundColor: '#fff',
      navigationBarTitleText: 'WeChat',
      navigationBarTextStyle: 'black'
    }
  }

  globalData = {
    wxUserInfo: null,
    kachaUserInfo:null
  }

  constructor () {
    super()
    this.use('promisify'); // 添加 promisify 使用 promise
    this.use('requestfix'); 
  }

  onLaunch() { 
    this.userAuth().then((res)=>{
      wepy.setStorage({
        key:"UserInfo",
        data:res
      })
    })
  }

  wxlogin() {
    return new Promise((resolve,reject)=>{
      wx.login({
        success:(res)=>{
          resolve(res)
        }
      })
    }) 
  }

  getUserInfo() {
    return new Promise((resolve,reject)=>{
      if(this.globalData.userInfo){
        resolve(this.globalData.userInfo)
      }else{
        wepy.getUserInfo().then((res)=>{ 
          resolve(res)
        })
      }
    }) 
  } 

  async userAuth(){
    let userInfo = null
    if(this.globalData.wxUserInfo && this.globalData.kachaUserInfo){
      userInfo = {wxUserInfo:this.globalData.wxUserInfo,kachaUserInfo:this.globalData.kachaUserInfo }
      return userInfo
    }else{
      const loginData = await this.wxlogin();
      const wxUserData = await this.getUserInfo(); 
      this.globalData.wxUserInfo = wxUserData.userInfo
      this.globalData.kachaUserInfo = await kachaAuthApi(loginData, wxUserData);
      return userInfo = {wxUserInfo:this.globalData.wxUserInfo,kachaUserInfo:this.globalData.kachaUserInfo }
    } 
  }


   


  /*
  sleep (s) {
    return new Promise((resolve, reject) => {
      setTimeout(() => {
        resolve('promise resolved')
      }, s * 1000)
    })
  } 
  async testAsync () {
    const data = await this.sleep(3)
    console.log(data)
  }
  */
}
</script>
